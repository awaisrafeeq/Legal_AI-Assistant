name: Deploy WhatsApp Bot to Azure

on:
  push:
    branches: [ main, master, task/code-refactor ]
    paths:
      - 'whatsapp_bot.py'
      - 'requirements-whatsapp.txt'
      - 'startup_whatsapp.txt'
      - '.github/workflows/deploy-whatsapp.yml'
  workflow_dispatch:

env:
  AZURE_WEBAPP_NAME: legal-ai-whatsapp
  PYTHON_VERSION: '3.11'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: ${{ env.PYTHON_VERSION }}

    - name: Create virtual environment and install dependencies
      run: |
        python -m venv antenv
        source antenv/bin/activate
        pip install --upgrade pip
        pip install -r requirements-whatsapp.txt
        deactivate

    - name: Create deployment package
      run: |
        zip -r deploy.zip \
          antenv/ \
          whatsapp_bot.py \
          prompts.py \
          requirements-whatsapp.txt \
          startup_whatsapp.txt \
          logs/ \
          -x "*.pyc" "__pycache__/*" "venv/*" ".venv/*"

    - name: Deploy to Azure Web App
      uses: azure/webapps-deploy@v3
      with:
        app-name: ${{ env.AZURE_WEBAPP_NAME }}
        publish-profile: ${{ secrets.AZURE_WHATSAPP_PUBLISH_PROFILE }}
        package: deploy.zip
